import { Board } from '../board'
import { BoardSolution } from './solution'

export class BruteSolver {
    findUniqueSolution(board: Board): BoardSolution {
        // Find 
        throw new Error("No unique solution")
    }
}